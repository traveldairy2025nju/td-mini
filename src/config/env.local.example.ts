// 本地环境配置文件示例
// 开发者可以复制此文件为 env.local.ts 并根据自己的本地环境进行修改
// env.local.ts 文件已被添加到 .gitignore，不会被提交到代码仓库

// 覆盖默认配置
const LOCAL_CONFIG = {
  BASE_URL: 'http://localhost:3000', // 修改为你的本地开发服务器地址
};

export default LOCAL_CONFIG; 