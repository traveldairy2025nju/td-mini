import CommentSection from './index.tsx';
import CommentInput from './CommentInput';
import { Comment, CommentAction, CommentSectionProps } from './interfaces';

export {
  CommentSection,
  CommentInput,
  Comment,
  CommentAction,
  CommentSectionProps
};

export default CommentSection; 